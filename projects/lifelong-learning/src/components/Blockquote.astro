<blockquote>
  <p>“</p>
  <slot />
</blockquote>
<style lang="scss">
  blockquote {
    margin: 0;
    padding: 0;
    color: var(--text);
    font-size: clamp(1.15rem, 2vw, 1.4rem);
    font-weight: 600;
    line-height: 1.6;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }
</style>
