<section class="not-found">
  <div class="not-found__content">
    <div class="not-found__icon">
      <i class="fa-solid fa-triangle-exclamation"></i>
    </div>

    <h1 class="not-found__code">404</h1>
    <h2 class="not-found__title">Página não encontrada</h2>

    <p class="not-found__description">
      O recurso que você está tentando acessar não existe ou foi movido para um
      novo endereço.
    </p>

    <div class="not-found__actions">
      <a href="/" class="btn-primary">
        <i class="fa-solid fa-house"></i>
        <span>Voltar ao Início</span>
      </a>
    </div>
  </div>
</section>

<style lang="scss">
  .not-found {
    text-align: center;
    padding: 2rem;

    &__content {
      max-width: 400px;
      margin-inline: auto;
      display: grid;
      gap: 1.5rem;
    }

    &__icon {
      font-size: 4rem;
      color: var(--red);
      margin-bottom: -1rem;
      filter: drop-shadow(0 0 15px rgba(243, 139, 168, 0.2));
    }

    &__code {
      font-size: 6rem;
      font-weight: 900;
      line-height: 1;
      margin: 0;
      background: linear-gradient(135deg, var(--text) 0%, var(--red) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    &__title {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text);
    }

    &__description {
      color: var(--subtext-0);
      font-size: 1rem;
      line-height: 1.6;
    }

    &__actions {
      margin-top: 1rem;
    }

    .btn-primary {
      text-decoration: none;
      padding: 0.85rem 2rem;
      border-radius: 0.75rem;
      font-weight: 700;
      font-size: 0.95rem;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.65rem;
      background-color: var(--surface-0);
      color: var(--text);
      border: 1px solid var(--border);
      transition: all 0.2s ease;

      &:hover {
        background-color: var(--surface-1);
        border-color: var(--primary);
        transform: translateY(-2px);
      }

      i {
        font-size: 1rem;
      }
    }
  }
</style>
