<div
  id="file-upload"
  class="group relative cursor-pointer overflow-hidden rounded-xl border-2 border-primary/50 bg-linear-to-br from-background to-muted/50 transition-all duration-300 hover:border-primary hover:from-muted/50 hover:to-background"
>
  <input
    type="file"
    id="file-input"
    class="hidden"
    accept=".pdf,.xlsx,.xls,.docx,.doc,.txt,.html,.xml,.csv"
  />

  <div class="flex flex-col items-center justify-center px-8 py-10 text-center">
    <div
      id="upload-icon"
      class="mb-6 transition-all duration-300 group-hover:scale-125 group-hover:-translate-y-1"
    >
      <svg
        class="size-14 text-primary drop-shadow-lg"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        stroke-width="1.5"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"
        ></path>
      </svg>
    </div>

    <h3 id="upload-title" class="mb-2 text-xl font-semibold text-foreground">
      Solte um arquivo para converter
    </h3>

    <p id="upload-subtitle" class="mb-6 text-sm text-muted-foreground">
      ou clique para procurar
    </p>
    <div
      id="format-badges"
      class="flex flex-wrap items-center justify-center gap-2"
    >
      {
        ["PDF", "XLSX", "DOCX", "TXT", "HTML", "XML", "CSV"].map((format) => (
          <span class="rounded-lg bg-white px-3 py-1.5 text-sm text-foreground shadow-sm">
            {format}
          </span>
        ))
      }
    </div>
  </div>
</div>

<script>
  import { mockConvertFile } from "../lib/mock-data";

  const uploadArea = document.getElementById("file-upload");
  const fileInput = document.getElementById("file-input") as HTMLInputElement;
  const uploadIcon = document.getElementById("upload-icon");
  const uploadTitle = document.getElementById("upload-title");
  const uploadSubtitle = document.getElementById("upload-subtitle");
  const formatBadges = document.getElementById("format-badges");
</script>
